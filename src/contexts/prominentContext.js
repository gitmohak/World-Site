// Creating the prominentContext - to get the scroll position of various components and mark them in Navbar for scrollSpy. Context API.
import { createContext } from "react";

const prominentContext = createContext();

export default prominentContext;