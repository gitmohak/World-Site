// Creating the stopScrollContext - to stop scrolling when the user enables dark mode. Context API.
import { createContext } from "react";

const stopScrollContext = createContext();

export default stopScrollContext;